# Rien de compilé n'entre ici. C'est la règle 1 de CLAUDE.md, et la raison
# pour laquelle l'ancien dépôt avait trois copies du site.
node_modules/
.next/
out/
dist/
dist-demo/
web-build/
.expo/
expo-env.d.ts

# Les secrets réels. Les clés publiques, elles, sont versionnées exprès :
# web/.env.production explique pourquoi.
.env
.env*.local

.DS_Store
*.tsbuildinfo

# Las sondas de `herramientas/` necesitan playwright-core; se instala a mano
# donde se vayan a correr y no se versiona.
herramientas/node_modules/
herramientas/tiros/
